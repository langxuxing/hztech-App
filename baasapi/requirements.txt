# BaasAPI 运行时依赖（deploy2Local / run_local / AWS restart 与 install_on_aws 均使用本文件）
flask>=3.0.0
PyJWT>=2.8.0   # import 时使用: import jwt
requests>=2.28.0
ccxt>=4.4.85
psycopg2-binary>=2.9.9
# pysqlite3-binary：仅在没有 _sqlite3 的环境（如部分 Linux）需手动安装
# 运行 test/ 下 pytest：pip install -r test/requirements.txt
