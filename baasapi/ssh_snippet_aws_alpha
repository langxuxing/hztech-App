# 方式 2：将下面 Host 整段复制到本机 ~/.ssh/config 末尾（或合并进已有文件）。
# 若已存在 Host aws-alpha，只改 HostName / IdentityFile 即可。
# 路径与 baasapi/deploy-aws.json 中 baasapi 段一致；密钥请 chmod 600。

Host aws-alpha
  HostName 54.66.108.150
  User ec2-user
  Port 22
  IdentityFile /Volumes/HZTech/aws-sydney/hztech.pem
  IdentitiesOnly yes
  StrictHostKeyChecking accept-new
